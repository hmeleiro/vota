# Validate District Patterns

Validates that IDV names in district patterns are valid.

## Usage

``` r
check_patrones(patrones, idv_lvls)
```

## Arguments

- patrones:

  Data frame with district patterns

- idv_lvls:

  Vector with valid IDV codes

## Value

Invisible(TRUE) if valid, or error if not
